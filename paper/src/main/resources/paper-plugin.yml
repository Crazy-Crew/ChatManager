name: '${name}'
main: '${group}.${name}'
loader: '${group}.api.runtime.DependencyLoader'

version: '${version}'
api-version: '${minecraft}'
description: '${description}'

folia-supported: true

dependencies:
  server:
    PlaceholderAPI:
     load: BEFORE
     required: false
     join-classpath: true